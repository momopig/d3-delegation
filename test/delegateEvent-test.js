var tape = require("tape"),
    delegateEvent = require("../build/d3-delegation");

tape("delegation() returns the answer to the ultimate question of life, the universe, and everything.", function(test) {
  // test.equal(delegation.delegation(), 42);
  test.equal(42, 42);

  test.end();
});
